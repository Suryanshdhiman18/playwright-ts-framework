{
  "name": "playwright-ts-framework",
  "version": "1.0.0",
  "main": "index.js",
"scripts": {
    "test": "playwright test",

    "auth": "playwright test tests/setup/auth.setup.ts",

    "p360": "playwright test tests/p360 --project=chromium",

    "catalog": "playwright test tests/catalog --project=chromium",

    "smoke": "playwright test tests/p360 tests/catalog --project=chromium",

    "regression": "playwright test --project=chromium",

    "report": "playwright show-report",

    "report:port": "playwright show-report --port=9324"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.9.1"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "exceljs": "^4.4.0",
    "xlsx": "^0.18.5"
  }
}
